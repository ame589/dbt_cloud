sources:
  - name: orders
    schema: zurich_at_bronze
    tables:
      - name: bronze_orders
        columns:
          - name: order_id
            tests: [not_null, unique]
          - name: order_date_time
            tests: [ not_null ]
